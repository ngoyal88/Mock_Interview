import { COMPANY_LABELS } from 'shared/reference/companies';
import { ROLE_LABELS } from 'shared/reference/roles';

/** Shared product company labels — slugs live on SUPPORTED_COMPANIES.id. */
export const COMPANY_OPTIONS = COMPANY_LABELS;

/** Shared product role labels. */
export const ROLE_OPTIONS = ROLE_LABELS;
